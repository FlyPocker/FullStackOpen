{
  "scripts": {
    "dev": "concurrently \"npm run dev --prefix blog-backend\" \"npm run dev --prefix bloglist-frontend\"",
    "build": "npm run build --prefix bloglist-frontend",
    "start": "NODE_ENV=production npm start --prefix blog-backend"
  },
  "devDependencies": {
    "concurrently": "^8.0.0",
    "prettier": "^3.9.6"
  }
}
